This folder holds archived files that are not part of the active app build.

The active application lives in `src/` and is mounted from `index.html` via `src/main.tsx`.

Archived here:
- `App.tsx` from the repo root
- `relevance-agent-tracker-v2-main/`
- `relevance-agent-tracker-v2-main.zip`

These files were moved here to reduce project confusion without deleting anything.
